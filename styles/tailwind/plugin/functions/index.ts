import { gradientFading } from './_gradientFunction'
import { blurredColor } from './_blurFunction'
import { dimColor } from './_dim'

const functions = {
  'gradient-fading': gradientFading,
  'blurred-color': blurredColor,
  dim: dimColor,
}

export default functions
